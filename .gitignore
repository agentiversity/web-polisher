# Dependencies
node_modules/

# pi local configuration (credentials, tooling, skills) — machine-local, not shared
.pi/

# Local agent/tooling config (commands, skills, caches, credentials) — machine-local, not shared
.omp/
.opencode/
.serena/
piolium/

# Planning research cache (regenerated)
docs/research/.cache/

# Build output
dist/
.output/
*.tsbuildinfo

# Logs
*.log
npm-debug.log*

# Local environment / secrets (never commit)
.env
.env.*

# OS / editor
.DS_Store
Thumbs.db

# WXT generated directory
.wxt/
