# Candidate Scan

Generated by piolium at 2026-08-04T03:07:47.175Z

## Totals

- Files scanned: 23
- Candidate files: 11
- Candidate matches: 37
- Per-file records: disabled (set PIOLIUM_FILE_RECORDS=1 to enable)

## Candidate Classes

- hidden-control-channel: 19 match(es), max score 55. Request header or framework/proxy context read that may influence auth, routing, tenant, runtime, debug, or middleware behavior.
- unsafe-html-or-template: 10 match(es), max score 55. HTML injection sink or template escape bypass.
- ssrf-capable-request: 5 match(es), max score 55. Outbound HTTP request site that may be attacker-controlled.
- path-traversal-file-access: 2 match(es), max score 55. Filesystem access using path joins or user-controllable paths.
- raw-sql-query: 1 match(es), max score 55. Raw SQL construction or query execution that may need parameterization review.

## Top Files

- `utils/textReplacer.test.ts`: score 440, 8 match(es)
- `utils/contentDetector.test.ts`: score 330, 6 match(es)
- `utils/live.integration.test.ts`: score 330, 6 match(es)
- `utils/domWalk.ts`: score 275, 5 match(es)
- `utils/polish.test.ts`: score 220, 4 match(es)
- `utils/contentDetector.ts`: score 110, 2 match(es)
- `utils/llmClient.ts`: score 110, 2 match(es)
- `entrypoints/background.ts`: score 55, 1 match(es)
- `entrypoints/options/index.ts`: score 55, 1 match(es)
- `utils/settings.ts`: score 55, 1 match(es)
- `utils/textReplacer.ts`: score 55, 1 match(es)

## Highest-Ranked Matches

- hidden-control-channel (normal, score 55) at `entrypoints/background.ts:9` - * In MV3 this is the only place that may make cross-origin requests (content
- unsafe-html-or-template (normal, score 55) at `entrypoints/options/index.ts:39` - await browser.storage.local.set({ [API_KEY_STORAGE_KEY]: raw });
- unsafe-html-or-template (normal, score 55) at `utils/contentDetector.test.ts:11` - document.body.innerHTML = html;
- unsafe-html-or-template (normal, score 55) at `utils/contentDetector.test.ts:17` - document.body.innerHTML = '';
- hidden-control-channel (normal, score 55) at `utils/contentDetector.test.ts:123` - // Simulate www.reddit.com: a shreddit-comment host whose visible body text
- hidden-control-channel (normal, score 55) at `utils/contentDetector.test.ts:125` - const host = document.createElement('shreddit-comment');
- hidden-control-channel (normal, score 55) at `utils/contentDetector.test.ts:126` - const shadow = host.attachShadow({ mode: 'open' });
- hidden-control-channel (normal, score 55) at `utils/contentDetector.test.ts:130` - document.body.append(host);
- hidden-control-channel (normal, score 55) at `utils/contentDetector.ts:125` - function normalize(host: string): string {
- hidden-control-channel (normal, score 55) at `utils/contentDetector.ts:126` - return host.toLowerCase().replace(/^www\./, '').replace(/:\d+$/, '');
- hidden-control-channel (normal, score 55) at `utils/domWalk.ts:24` - // Reached the top of a shadow tree — hop across to its light-DOM host and
- hidden-control-channel (normal, score 55) at `utils/domWalk.ts:25` - // keep climbing, so we see whether `ancestor` wraps the whole shadow host.
- hidden-control-channel (normal, score 55) at `utils/domWalk.ts:28` - n = root.host;
- hidden-control-channel (normal, score 55) at `utils/domWalk.ts:39` - // The root itself may be a shadow host whose shadowRoot holds the visible text.
- hidden-control-channel (normal, score 55) at `utils/domWalk.ts:103` - node = root instanceof ShadowRoot ? root.host : null;
- ssrf-capable-request (normal, score 55) at `utils/live.integration.test.ts:17` - * Node's AbortSignal — Node's undici fetch (underneath @google/generative-ai)
- ssrf-capable-request (normal, score 55) at `utils/live.integration.test.ts:83` - await fetch('https://hacker-news.firebaseio.com/v0/topstories.json')
- ssrf-capable-request (normal, score 55) at `utils/live.integration.test.ts:86` - await fetch(`https://hacker-news.firebaseio.com/v0/item/${top[0]}.json`)
- ssrf-capable-request (normal, score 55) at `utils/live.integration.test.ts:91` - fetch(`https://hacker-news.firebaseio.com/v0/item/${k}.json`).then((r) => r.json()),
- unsafe-html-or-template (normal, score 55) at `utils/live.integration.test.ts:105` - document.body.innerHTML = `
- path-traversal-file-access (normal, score 55) at `utils/live.integration.test.ts:114` - .join('')}
- hidden-control-channel (normal, score 55) at `utils/llmClient.ts:4` - * Content scripts cannot make cross-origin fetches in MV3, so all Gemini API
- path-traversal-file-access (normal, score 55) at `utils/llmClient.ts:47` - ].join(' ');
- unsafe-html-or-template (normal, score 55) at `utils/polish.test.ts:16` - document.body.innerHTML =
- unsafe-html-or-template (normal, score 55) at `utils/polish.test.ts:26` - document.body.innerHTML = '';
- unsafe-html-or-template (normal, score 55) at `utils/polish.test.ts:40` - document.body.innerHTML = '<article><p>hi</p><p>a sufficiently long comment text here</p></article>';
- unsafe-html-or-template (normal, score 55) at `utils/polish.test.ts:48` - document.body.innerHTML =
- ssrf-capable-request (normal, score 55) at `utils/settings.ts:24` - /** Max texts per single API request (bounded batch, design D2). */
- unsafe-html-or-template (normal, score 55) at `utils/textReplacer.test.ts:14` - document.body.innerHTML = bodyHtml;
- unsafe-html-or-template (normal, score 55) at `utils/textReplacer.test.ts:20` - document.body.innerHTML = '';
- hidden-control-channel (normal, score 55) at `utils/textReplacer.test.ts:101` - it('replaces text inside shadow roots (Reddit shreddit host)', () => {
- hidden-control-channel (normal, score 55) at `utils/textReplacer.test.ts:103` - // shreddit-comment host. Plain TreeWalkers never see it; the shadow-aware
- hidden-control-channel (normal, score 55) at `utils/textReplacer.test.ts:105` - const host = document.createElement('shreddit-comment');
- hidden-control-channel (normal, score 55) at `utils/textReplacer.test.ts:106` - const shadow = host.attachShadow({ mode: 'open' });
- hidden-control-channel (normal, score 55) at `utils/textReplacer.test.ts:112` - document.body.append(host);
- hidden-control-channel (normal, score 55) at `utils/textReplacer.test.ts:114` - const n = replaceTextNodes(host, { transform: (t) => `[p] ${t}` });
- raw-sql-query (normal, score 55) at `utils/textReplacer.ts:127` - 'button, [role="button"], a, input, textarea, select, label, form, ' +

## Custom Matchers

Project matchers can be added at `piolium/matchers.json`, `piolium/custom-matchers.json`, or `.piolium-matchers.json`.
